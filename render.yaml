services:
  - type: web
    name: falix-remote-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: FALIX_EMAIL
        sync: false
      - key: FALIX_PASSWORD
        sync: false
